$(function(){
  
  $('.post-detail').bind('ajax:success', function() {
     var contact = $(this).data('post');
        
     $('#postModal #post-title').text(contact.title);
     $('#postModal #post-body').text(contact.body);
     $('#postModal').modal('show');
  });
  
});